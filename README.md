# Frontend (Vite + React + TypeScript)

Comandos para arrancar el proyecto (PowerShell):

```powershell
cd frontend
npm install
npm run dev
```

El dev server arrancará por defecto en `http://localhost:3000`.
